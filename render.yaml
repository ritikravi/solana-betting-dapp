services:
  - type: web
    name: solana-betting-backend
    runtime: node
    buildCommand: cd backend && chmod +x render-build.sh && ./render-build.sh
    startCommand: cd backend && npm start
    rootDir: /
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
